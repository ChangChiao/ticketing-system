apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticketing-ws
  labels:
    app: ticketing-ws
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ticketing-ws
  template:
    metadata:
      labels:
        app: ticketing-ws
    spec:
      initContainers:
        - name: migrate
          image: ticketing-api:latest
          command: ["/migrate"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ticketing-secrets
                  key: database-url
            - name: MIGRATIONS_DIR
              value: /migrations
      containers:
        - name: ws
          image: ticketing-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ticketing-secrets
                  key: database-url
            - name: REDIS_ADDR
              value: redis-cluster:6379
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ticketing-secrets
                  key: jwt-secret
            - name: LINEPAY_CHANNEL_ID
              valueFrom:
                secretKeyRef:
                  name: ticketing-secrets
                  key: linepay-channel-id
            - name: LINEPAY_CHANNEL_SECRET
              valueFrom:
                secretKeyRef:
                  name: ticketing-secrets
                  key: linepay-channel-secret
            - name: LINEPAY_BASE_URL
              value: https://sandbox-api-pay.line.me
            - name: APP_BASE_URL
              value: https://tickets.example.com
            - name: TURNSTILE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: ticketing-secrets
                  key: turnstile-secret-key
            - name: REQUEST_SIGN_SECRET
              valueFrom:
                secretKeyRef:
                  name: ticketing-secrets
                  key: request-sign-secret
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: ticketing-ws
spec:
  selector:
    app: ticketing-ws
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ticketing-ws-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ticketing-ws
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
